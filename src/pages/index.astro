---
/**
 * PLACEHOLDER — Cursor CLI replaces this entire file.
 *
 * This placeholder exists so the base template is a valid Astro project
 * that can build and deploy. Cursor CLI will generate the real landing page
 * based on the design mockup and content data.
 */
import Layout from '../layouts/Layout.astro';
import siteData from '../../content/site.json';
---

<Layout>
  <main class="flex items-center justify-center min-h-screen bg-gray-50">
    <div class="text-center max-w-2xl mx-auto px-6 py-20">
      <h1 class="text-4xl font-bold text-gray-900 mb-4">
        {siteData.business_name || 'Your Business'}
      </h1>
      <p class="text-xl text-gray-600 mb-8">
        {siteData.headline || 'Website coming soon'}
      </p>
      <p class="text-sm text-gray-400">
        This site is being generated by Website Factory.
        Check back shortly for the full experience.
      </p>
    </div>
  </main>
</Layout>
